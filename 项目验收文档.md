# 校园学生积分互助网站（Help-1）— 项目验收文档

---

## 一、项目内容介绍

Help-1 是一个**校园学生积分互助平台**，旨在为在校学生提供便捷的互助委托发布与接取服务。平台以积分为核心激励媒介，学生可以通过发布互助任务（如取快递、课程辅导、设备维修等）悬赏积分，其他学生接单完成后获得相应积分奖励。同时，平台配备教师管理端，负责内容审核、用户管理、积分调控等运营工作。

项目采用**前后端分离架构**，前端基于 React 18 + TypeScript + Vite 构建，后端基于 Spring Boot 3.2 + MyBatis-Plus 实现，数据库采用 MySQL 8.0，通过 Docker Compose 实现容器化编排并部署于阿里云 ECS 云服务器。平台集成了 DeepSeek AI 大模型，提供 AI 页面导览等智能化功能。

**项目仓库地址**：[https://github.com/AASSKK99881/Help-1](https://github.com/AASSKK99881/Help-1)

**线上部署地址**：
- 前端页面：http://121.199.22.22
- 后端 API：http://121.199.22.22:8080
- 健康检查：http://121.199.22.22:8080/health
- 监控指标：http://121.199.22.22:8080/metrics

---

## 二、项目功能介绍

### 2.1 学生端功能

| 功能模块 | 详细说明 |
|----------|----------|
| 登录/注册 | 支持学号实名制注册与登录，JWT Token 鉴权，BCrypt 密码加密 |
| 任务大厅 | 首页展示可接取的互助任务列表，支持分类筛选、关键词搜索、骨架屏加载 |
| 任务详情 | 查看任务完整信息（标题、描述、分类、截止时间、悬赏积分），接单后展示双方联系方式 |
| 发布任务 | 标准化表单填写，支持设置悬赏积分、匿名发布，可调用 AI 辅助填写 |
| 我的委托 | 查看已发布和已接取的委托列表，支持确认完成、取消委托、放弃接单等操作 |
| 个人中心 | 个人资料编辑、积分余额查看、我的活动记录 |
| 积分明细 | 积分流水查询，包含收支统计 |
| 消息中心 | 系统通知查看与管理 |
| 活动中心 | 浏览校园活动列表、在线报名 |
| 账号设置 | 修改密码等安全设置 |
| AI 页面导览 | 悬浮按钮触发 AI 页面功能概括引导（DeepSeek-V3） |

### 2.2 管理端功能

| 功能模块 | 详细说明 |
|----------|----------|
| 管理员登录 | 管理员专属登录入口，自动识别角色跳转管理后台 |
| 数据看板 | 平台总用户数、活跃用户、累计委托量、待审核数等核心指标可视化 |
| 内容审核 | 待审核/已通过/已驳回分类管理，支持单条/批量审核操作，驳回需填写原因 |
| 用户管理 | 用户列表查看、多维度筛选、积分调整（发放/扣除）、账号封禁/解封 |
| 活动管理 | 发布与管理校园活动，自动同步至学生端轮播 Banner 与活动列表 |
| 系统设置 | 平台参数配置与管理 |

### 2.3 核心业务流程

平台的核心业务流程为：**任务发布 → AI+关键词双模审核 → 任务上架 → 学生接单 → 任务执行 → 确认完成 → 积分划转**。

积分流转规则：
- 发布任务时预冻结相应积分
- 任务完成时积分从冻结状态划转至接单者账户
- 取消委托时按违约规则扣除发布者一定比例积分
- 所有积分变动均产生流水记录（points_logs），保证可追溯性

---

## 三、项目层次结构

### 3.1 系统架构总览

```
┌─────────────────────────────────────────────────┐
│                    用户浏览器                      │
│         http://121.199.22.22 (前端页面)            │
└─────────────┬───────────────────┬───────────────┘
              │                   │
     ┌────────▼────────┐  ┌──────▼──────┐
     │   Nginx (React) │  │  Spring Boot│
     │   :8080→80      │  │  :8080      │
     └─────────────────┘  └──────┬──────┘
                                 │
                          ┌──────▼──────┐
                          │   MySQL 8.0 │
                          │   :3306     │
                          └─────────────┘
```

### 3.2 前端目录结构

```
frontend/
├── public/                        # 静态资源
├── src/
│   ├── app/
│   │   ├── api/                   # Axios 接口统一封装
│   │   │   ├── client.ts          # HTTP 客户端（拦截器、Token 携带）
│   │   │   ├── auth.ts            # 认证接口
│   │   │   ├── tasks.ts           # 任务接口
│   │   │   ├── user.ts            # 用户接口
│   │   │   └── activities.ts      # 活动接口
│   │   ├── components/            # 可复用 UI 组件
│   │   │   ├── TaskCard.tsx        # 任务卡片
│   │   │   ├── TaskStatusBadge.tsx # 任务状态标签
│   │   │   ├── PointsBadge.tsx     # 积分标识
│   │   │   └── AIPageSummary.tsx   # AI 页面导览
│   │   ├── contexts/              # React Context
│   │   │   └── AuthContext.tsx     # 全局认证状态
│   │   ├── layouts/               # 页面布局
│   │   │   ├── StudentLayout.tsx   # 学生端布局
│   │   │   └── AdminLayout.tsx     # 管理端布局
│   │   ├── pages/                 # 页面组件
│   │   │   ├── student/           # 学生端页面
│   │   │   │   ├── Login.tsx
│   │   │   │   ├── Home.tsx
│   │   │   │   ├── TaskDetail.tsx
│   │   │   │   ├── CreateTask.tsx
│   │   │   │   ├── MyTasks.tsx
│   │   │   │   ├── Profile.tsx
│   │   │   │   ├── PointsHistory.tsx
│   │   │   │   ├── Messages.tsx
│   │   │   │   ├── Activities.tsx
│   │   │   │   └── Settings.tsx
│   │   │   └── admin/             # 管理端页面
│   │   │       ├── Login.tsx
│   │   │       ├── Dashboard.tsx
│   │   │       ├── UserManagement.tsx
│   │   │       ├── ContentReview.tsx
│   │   │       ├── ActivityManagement.tsx
│   │   │       └── SystemSettings.tsx
│   │   ├── App.tsx                # 根组件
│   │   └── routes.tsx             # 路由配置
│   ├── tests/                     # 测试文件（17个测试文件）
│   └── __mocks__/                 # Mock 数据
├── Dockerfile                     # 多阶段构建（Node构建 + Nginx部署）
├── nginx.conf                     # Nginx 非 root 配置
├── package.json
├── vite.config.ts
├── tsconfig.json
└── .eslintrc.cjs
```

### 3.3 后端目录结构

```
backend/help/
├── src/main/java/com/help/
│   ├── HelpApplication.java       # Spring Boot 启动类
│   ├── common/
│   │   └── Result.java            # 统一响应封装
│   ├── config/
│   │   ├── AuthInterceptor.java   # 认证拦截器（JWT Token 校验）
│   │   ├── AdminInterceptor.java  # 管理员权限拦截器
│   │   ├── JwtUtil.java           # JWT 工具类（签发/验证）
│   │   ├── MetricsInterceptor.java # 请求指标拦截器
│   │   ├── GlobalExceptionHandler.java # 全局异常处理
│   │   ├── MybatisConfig.java     # MyBatis-Plus 配置
│   │   ├── RestTemplateConfig.java # HTTP 客户端配置
│   │   ├── StartupRunner.java     # 启动初始化
│   │   └── WebMvcConfig.java      # MVC 配置（拦截器注册）
│   ├── controller/
│   │   ├── AuthController.java    # 认证接口（登录/注册）
│   │   ├── TaskController.java    # 任务接口（CRUD + 业务动作）
│   │   ├── UserController.java    # 用户信息接口
│   │   ├── StudentActivityController.java # 学生活动接口
│   │   ├── AdminController.java   # 管理端接口
│   │   ├── AdminActivityController.java # 管理端活动接口
│   │   ├── HealthController.java  # 健康检查端点
│   │   └── MetricsController.java # 指标查询端点
│   ├── entity/
│   │   ├── User.java              # 用户实体
│   │   ├── Task.java              # 任务实体
│   │   ├── Activity.java          # 活动实体
│   │   ├── ActivityParticipant.java # 活动参与实体
│   │   ├── PointsLog.java         # 积分流水实体
│   │   └── SensitiveWord.java     # 敏感词实体
│   ├── mapper/
│   │   ├── UserMapper.java
│   │   ├── TaskMapper.java
│   │   ├── ActivityMapper.java
│   │   ├── ActivityParticipantMapper.java
│   │   ├── PointsLogMapper.java
│   │   └── SensitiveWordMapper.java
│   └── service/
│       ├── TaskService.java       # 任务业务接口
│       ├── AiService.java         # AI 服务接口
│       └── impl/
│           ├── TaskServiceImpl.java # 任务业务实现（积分划转事务）
│           └── AiServiceImpl.java   # AI 服务实现（DeepSeek API 调用）
├── src/main/resources/
│   ├── application.properties     # 应用配置（数据库、JWT、AI Key）
│   └── logback-spring.xml         # 结构化日志配置
├── src/test/java/com/help/        # 测试代码
├── Dockerfile                     # 多阶段构建（Maven编译 + JRE运行）
├── pom.xml
└── settings.xml                   # Maven 阿里云镜像加速
```

---

## 四、产品开发与迭代流程

本项目的开发过程分为 **12 个阶段**，每次迭代聚焦一个专项任务，逐步完善系统功能与质量保障。

| 阶段 | 主题 | 主要内容 | 完成时间 |
|------|------|----------|----------|
| 01 | 项目初始化 | Git 仓库建立、团队分工、CLAUDE.md 编写 | 2026-03-10 |
| 02 | UI/UX 原型设计 | Figma 高保真原型（学生端 + 管理端），Material Design 3 规范，交互说明文档 | 2026-03-17 |
| 03 | 软件架构设计 | 前后端架构图（Mermaid）、技术选型（Vite/Spring Boot/MySQL）、数据库 ER 图设计、系统交互流程图 | 2026-03-24 |
| 04 | API 设计与实现 | RESTful API 规范（OpenAPI 文档）、前端 Axios 封装 + MSW Mock、后端路由定义 + 业务逻辑 | 2026-03-31 |
| 05 | 前端页面开发 | 学生端全部页面开发（登录/注册/首页/详情/个人中心/发布/消息/活动/设置），组件封装（TaskCard/PointsBadge/TaskStatusBadge） | 2026-04-11 |
| 06 | 后端业务开发 | 后端完整 CRUD 实现、用户认证、任务流程（发布→接单→完成→积分划转）、Dockerfile 编写 | 2026-04-14 |
| 07 | AI 功能集成 | DeepSeek-V3 集成：后端封装 `/api/ai/summary` 端点、前端 AIPageSummary 悬浮按钮组件、Prompt Engineering、API Key 环境变量管理 | 2026-04-20 |
| 08 | 软件测试 | 前端 17 个测试文件 / 111 个测试用例（Vitest）：组件渲染测试 45 个 + Mock API 测试 39 个 + 拦截器测试 24 个；后端单元测试 + API 测试；整体语句覆盖率 83.47% | 2026-04-23 |
| 09 | CI/CD 配置 | GitHub Actions 流水线：CI（前后端并行测试 + Codecov 覆盖率上传）、Docker（自动构建推送到 GHCR + Trivy 安全扫描） | 2026-05-05 |
| 10 | 安全审查 | AI 辅助安全审查：修复 IDOR 越权漏洞、权限提升漏洞、数据库明文密码硬编码；前端引入 DOMPurify XSS 防护、Token 安全存储；Gitleaks 密钥泄露扫描 | 2026-05-06 |
| 11 | Docker 容器化 | 前后端多阶段 Dockerfile（非 root 运行）、开发/生产/服务器三种 Compose 编排、健康检查、Docker Secrets 密钥管理、GHCR 镜像推送 | 2026-05-19 |
| 12 | 云服务部署 + 监控 | 阿里云 ECS 部署、安全组配置、compose.server.yaml 生产编排、结构化日志（LogstashEncoder）、健康检查端点、请求指标收集（请求计数/响应时间/错误率） | 2026-06-03 |

### 迭代流程特点

- **前后端分离并行开发**：每个阶段前后端各自认领任务，通过 API 契约协作，互不阻塞
- **AI 辅助提效**：全程使用 Claude Code / DeepSeek 等 AI 工具辅助代码生成、安全审查、测试编写、文档编写
- **持续集成保障质量**：每次 Push 触发 GitHub Actions 自动运行测试、Lint 检查和覆盖率上传
- **渐进式交付**：从原型设计 → 架构规划 → 基础功能 → AI 集成 → 质量保障 → 容器化部署，逐步交付可用的产品

---

## 五、前后端具体接口介绍

### 5.1 接口总览

所有接口遵循 RESTful 架构风格，统一使用 `Result<T>` 响应封装：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

Base URL：`/api`

认证机制：JWT (JSON Web Token)，Header 格式 `Authorization: Bearer <token>`

### 5.2 认证模块 (`/api/auth`)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/auth/login` | 用户登录（支持学号/邮箱），返回 JWT Token 和用户信息 | 否 |
| POST | `/api/auth/register` | 学生注册（学号实名制），BCrypt 密码加密 | 否 |

### 5.3 任务模块 (`/api/tasks`)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/api/tasks` | 分页查询任务大厅列表（支持 keyword/category 筛选，仅展示 status=1 待接单） | 否 |
| GET | `/api/tasks/{id}` | 获取任务详情（接单后展示双方联系方式） | 是 |
| POST | `/api/tasks` | 发布新任务（表单提交，自动进入审核） | 是 |
| GET | `/api/tasks/my/published` | 查看我发布的任务列表 | 是 |
| GET | `/api/tasks/my/accepted` | 查看我接取的任务列表 | 是 |
| POST | `/api/tasks/{id}/accept` | 接单（校验：不能接自己的、状态必须待接） | 是 |
| POST | `/api/tasks/{id}/complete` | 确认完成（触发积分划转事务） | 是 |
| POST | `/api/tasks/{id}/cancel` | 取消委托（按规则扣除违约积分） | 是 |
| POST | `/api/tasks/{id}/abandon` | 放弃接单（任务重新开放） | 是 |

### 5.4 AI 模块 (`/api/ai`)

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/ai/summary` | AI 页面导览，传入 pageContext，返回智能概括文案 | 是 |

### 5.5 监控端点

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/health` | 健康检查，返回状态、时间戳、版本号 | 否 |
| GET | `/metrics` | 请求指标查询（总请求数、错误率、平均响应时间） | 否 |

### 5.6 任务状态机

```
┌──────────┐    审核通过    ┌──────────┐    学生接单    ┌──────────┐
│ 0-待审核  │ ────────────→ │ 1-待接单  │ ────────────→ │ 2-进行中  │
└──────────┘               └──────────┘               └──────────┘
      │                         │                         │
      │ 审核驳回                │ 发布者取消               │ 确认完成
      ↓                         ↓                         ↓
┌──────────┐               ┌──────────┐               ┌──────────┐
│ 4-已取消  │               │ 4-已取消  │               │ 3-已完成  │
└──────────┘               └──────────┘               └──────────┘
                                                           ↑
                                                  接单者放弃 │
                                                  (状态回到1) │
                                                         ┌──────────┐
                                                         │ 1-待接单  │
                                                         └──────────┘
```

---

## 六、安全审查与软件测试体系

### 6.1 安全审查

#### 6.1.1 发现与修复的高危漏洞

| 漏洞名称 | 漏洞类型 | 危害等级 | 所在文件 | 修复方案 |
|----------|----------|----------|----------|----------|
| 数据库密码明文硬编码 | 敏感信息泄露 (A07) | **高危** | docker-compose.yml | 改为 `${DB_ROOT_PASSWORD}` 环境变量注入 + `.env` 文件 + `.gitignore` |
| 接单接口越权漏洞 (IDOR) | 失效访问控制 (A01) | **高危** | TaskController.java | 废弃请求体中的 studentId，改为从 `HttpServletRequest` 认证上下文中获取当前用户 ID |
| 登录接口权限提升 | 认证机制失效 (A07) | **高危** | AuthController.java | 阻断前端传入 role 参数，强制后端赋予默认权限，从数据库读取真实角色 |
| Token 硬编码 | 认证机制失效 (A07) | **高危** | AuthController.java | 替换为真实的 JWT 签发逻辑（JwtUtil） |
| AI 输出 XSS 注入 | 注入攻击 (A03) | 中危 | AIPageSummary.tsx | 引入 DOMPurify 库进行内容净化，编写 `createSafeHTML` 函数过滤恶意标签 |

#### 6.1.2 OWASP Top 10 安全检查清单

- [x] **密码存储**：BCrypt 哈希加密存储，HTTPS 传输
- [x] **JWT/Session**：JWT 签发与验证，过期处理，Token 不存 localStorage
- [x] **接口鉴权**：AuthInterceptor + AdminInterceptor 双层拦截器保护
- [x] **越权访问**：acceptTask/cancelTask/abandonTask 均从认证上下文获取用户 ID
- [x] **SQL 注入防护**：MyBatis-Plus ORM 参数化查询自动防御
- [x] **XSS 防护**：AI 输出引入 DOMPurify 净化，前端使用 React JSX 默认转义
- [x] **API Key/密码**：所有密钥通过环境变量注入，不进入代码仓库
- [x] **依赖安全**：Trivy 镜像安全扫描（CRITICAL/HIGH），npm audit 依赖审计
- [x] **密钥泄露扫描**：Gitleaks 自动扫描（GitHub Actions security.yml）

#### 6.1.3 CI 安全扫描

- **Gitleaks**：每次 push/pull_request 自动运行密钥泄露扫描，检出所有历史 commit
- **Trivy**：每次 main 分支镜像构建后扫描 CRITICAL 和 HIGH 级别漏洞
- **npm audit**：前端依赖安全审计

### 6.2 软件测试体系

#### 6.2.1 前端测试（Vitest + @testing-library/react）

| 测试类型 | 文件数 | 用例数 | 覆盖内容 |
|----------|--------|--------|----------|
| 组件渲染/交互测试 | 9 | 45 | TaskCard, TaskStatusBadge, PointsBadge, Login, Register, Messages, Settings, CreateTask |
| Mock API 测试 | 6 | 39 | LoginMockApi, SettingsMockApi, PointsHistoryMockApi, ProfileMockApi, MyTasksMockApi, ActivitiesMockApi |
| API 模块/拦截器测试 | 2 | 24 | 认证拦截器、响应解包、401/404/500 错误处理、网络异常兜底 |
| **合计** | **17** | **111** | 全部通过 |

**覆盖率指标**：
- 语句覆盖率 (Statements): **83.47%** (303/363)
- 分支覆盖率 (Branches): **68.75%** (121/176)
- 函数覆盖率 (Functions): **74.28%** (130/175)
- 行覆盖率 (Lines): **85.01%** (295/347)

核心组件平均覆盖率 > 85%，远超 50% 的最低要求。

#### 6.2.2 后端测试（JUnit + MockMvc + Jacoco）

- 后端与 Spring Boot 生态深度整合，使用 MockMvc 进行 Controller 层集成测试
- 使用 Mockito 隔离 Service/Mapper 层依赖，确保测试独立性
- Jacoco 生成 XML 覆盖率报告，上传至 Codecov

#### 6.2.3 CI 自动化测试流程

GitHub Actions CI 工作流（`.github/workflows/ci.yml`）：
1. **Backend Job**：启动 MySQL 8.0 Service 容器 → 安装 JDK 17 → 执行 `mvn test`（生成 Jacoco 覆盖率） → 上传 Codecov（flag: backend）
2. **Frontend Job**：安装 Node.js 20 → `npm ci` → ESLint 检查 → Vitest 测试（生成 lcov 覆盖率） → 上传 Codecov（flag: frontend）

**README 状态徽章**：
[![CI](https://github.com/AASSKK99881/Help-1/actions/workflows/ci.yml/badge.svg)](https://github.com/AASSKK99881/Help-1/actions)
[![Backend Coverage](https://codecov.io/gh/AASSKK99881/Help-1/branch/main/graph/badge.svg?flag=backend)](https://codecov.io/gh/AASSKK99881/Help-1)
[![Frontend Coverage](https://codecov.io/gh/AASSKK99881/Help-1/branch/main/graph/badge.svg?flag=frontend)](https://codecov.io/gh/AASSKK99881/Help-1)

---

## 七、云服务与本地部署架构

### 7.1 部署平台

采用**阿里云 ECS + Docker Compose 统一编排**方案。

选择理由：
- 国内低延迟访问，新用户免费试用
- 一个 Compose 文件即可编排前端（Nginx）、后端（Spring Boot）、数据库（MySQL 8.0）三个服务
- 避免多平台拆分（Vercel + Railway）带来的跨域 CORS 配置和外部数据库连接问题

### 7.2 部署架构

```
用户浏览器
    │
    ├── http://121.199.22.22:80 ─── 前端 Nginx (React SPA)
    │                                 容器端口 8080 → 宿主机 80
    │
    └── http://121.199.22.22:8080 ─── 后端 Spring Boot
                │                       容器端口 8080 → 宿主机 8080
                │
                └── MySQL 8.0 (Docker 内网通信，不暴露外网)
                      容器端口 3306
```

### 7.3 服务端口映射

| 服务 | 容器内端口 | 宿主机端口 | 安全组状态 | 说明 |
|------|-----------|-----------|-----------|------|
| frontend | 8080 | 80 | 开放 | Nginx 非特权端口 8080，映射到宿主机 80 |
| backend | 8080 | 8080 | 开放 | Spring Boot 直接映射 |
| db | 3306 | 3306 | **不开放** | MySQL 仅 Docker 内网通信 |

### 7.4 Compose 文件分层策略

| 文件 | 用途 | 特点 |
|------|------|------|
| `compose.yaml` | 本地开发环境 | 热重载挂载、端口暴露、开发模式 |
| `compose.prod.yaml` | 生产环境 | GHCR 镜像拉取、Docker Secrets 密钥管理、资源限制（frontend 128M / backend 512M）、restart: unless-stopped |
| `compose.server.yaml` | 服务器部署 | 本地构建 + 环境变量注入（VITE_API_URL / DB_PASSWORD / AI_API_KEY）、阿里云 Maven 镜像加速 |

### 7.5 健康检查配置

- **MySQL**：`mysqladmin ping`，interval 5s，retries 10
- **Backend**：wget `/health` 端点，interval 30s，retries 3
- **Frontend**：wget 自身 8080 端口，interval 30s，retries 3

服务启动顺序：db (healthy) → backend (healthy) → frontend

### 7.6 本地部署步骤

```bash
# 1. 克隆仓库
git clone https://github.com/AASSKK99881/Help-1.git
cd Help-1

# 2. 创建环境变量文件
cp .env.example .env
# 编辑 .env 填入 DB_ROOT_PASSWORD、JWT_SECRET、AI_API_KEY

# 3. 一键启动（开发环境）
docker compose up -d --build

# 4. 访问
# 前端: http://localhost
# 后端: http://localhost:8080
# 健康检查: http://localhost:8080/health
```

### 7.7 Docker 安全实践

- **非 root 运行**：前端 Nginx 使用 `USER nginx`，后端使用 `USER spring`
- **非特权端口**：Nginx 监听 8080（非 80），避免 root 权限需求
- **多阶段构建**：构建阶段与运行阶段分离，最小化镜像体积（前端 ~28MB）
- **Docker Secrets**：生产环境密码通过 `/run/secrets/db_password` 文件挂载注入
- **内存限制**：生产环境设置 128M（frontend）和 512M（backend）内存上限
- **Trivy 安全扫描**：CI 流水线中自动扫描 CRITICAL/HIGH 级别漏洞

### 7.8 可观测性

| 组件 | 实现方式 | 说明 |
|------|----------|------|
| 健康检查 | `GET /health` | 返回 `{"status":"healthy","timestamp":"...","version":"1.0.0"}` |
| 结构化日志 | LogstashEncoder (Logback) | JSON 格式，控制台 + 文件双输出，按天滚动保留 7 天 |
| 请求指标 | `GET /metrics` | 总请求数、错误率、平均响应时间（MetricsInterceptor 基于 AtomicLong + ThreadLocal 实现） |
| Docker 健康检查 | Compose healthcheck | 基于 `/health` 端点的 wget 轮询 |

---

## 八、成员分工表

| 姓名 | 学号 | 角色 | 主要贡献 |
|------|------|------|----------|
| **纪嘉乐** | 2312190109 | 前端负责人 | 见下方 8.1 节 |
| **王敏涛** | 2312190125 | 后端负责人 | 见下方 8.2 节 |

### 8.1 纪嘉乐（前端负责人）

| 阶段 | 工作内容 |
|------|----------|
| UI 设计 | 学生端全部页面设计（登录/注册/首页/委托接取/个人委托/消息），Material Design 3 配色方案与字体规范制定，Figma 交互原型搭建与完整跳转逻辑补全 |
| 架构设计 | 前端架构设计（Vite + TypeScript 技术选型），前端项目初始化与环境搭建，CLAUDE.md 编写，README.md 更新 |
| API 设计 | 前端 HTTP 客户端配置（Axios），API 调用函数封装（auth/tasks/user/activities），Mock 数据配置（MSW） |
| 前端开发 | 全部学生端页面开发（首页/详情/个人中心/发布/消息/活动/设置），组件封装（TaskCard/PointsBadge/TaskStatusBadge），API 对接、加载状态与错误处理、sonner Toast 交互 |
| AI 集成 | 前端 AI 调用与错误处理，AIPageSummary 悬浮按钮组件开发 |
| 软件测试 | 编写 17 个测试文件 / 111 个测试用例（组件渲染 45 + Mock API 39 + 拦截器 24），整体语句覆盖率 83.47%，核心组件平均 > 85% |
| CI/CD | 参与编写 ci.yml，配置前端 ESLint + Vitest + Codecov（frontend flag），添加 README 状态徽章 |
| 安全审查 | 前端安全审查：DOMPurify XSS 防护、Token 安全存储优化、Axios 拦截器错误脱敏、Gitleaks 密钥扫描配置、防 CSRF 请求头 |
| Docker | 前端多阶段 Dockerfile（Node 构建 + Nginx 部署 + 非 root 运行），nginx.conf 适配，开发/生产/服务器三种 Compose 编排，健康检查配置，.dockerignore 编写，deploy.sh 一键部署脚本，.env.example 模板 |
| 云服务部署 | 阿里云 ECS 选型与部署，安全组配置，compose.server.yaml 编写，VITE_API_URL 构建时注入修复，数据库建表与中文乱码修复，Swap 内存扩容，部署文档编写 |
| 监控 | 审查 logback-spring.xml 结构化日志配置，审查 HealthController，审查 MetricsInterceptor（AtomicLong + ThreadLocal），monitoring.md 文档编写 |

### 8.2 王敏涛（后端负责人）

| 阶段 | 工作内容 |
|------|----------|
| UI 设计 | 管理端全部页面设计（登录/注册/数据看板/内容审核/系统设置/用户管理），管理后台配色方案制定，后台组件库（复杂表格/侧边导航/数据卡片/分页器）搭建 |
| 架构设计 | 后端架构设计（Spring Boot + MyBatis-Plus 技术选型），MySQL 数据库 ER 图设计与建表脚本编写，系统交互流程设计，后端项目初始化与数据库连接配置 |
| API 设计 | 用户认证 API、业务资源 API、查询接口设计，OpenAPI 文档编写，后端事务处理逻辑（积分划转/并发控制），CORS 跨域配置 |
| 后端开发 | 全部后端 API 实现（认证/任务 CRUD/积分流转），User/Task/PointsLog 实体类定义，MyBatis-Plus ORM 配置，Dockerfile 多阶段构建，统一错误响应封装 |
| AI 集成 | 后端 AI API 封装（`/api/ai/summary`），DeepSeek API HTTP 调用逻辑（OpenAI 协议），AI_API_KEY 环境变量安全管理，异常处理与参数校验 |
| 软件测试 | 后端单元测试与 API 测试（MockMvc + Mockito），Jacoco 覆盖率 |
| CI/CD | 参与编写 ci.yml（Maven 测试 + Codecov backend flag），Mockito 隔离数据库依赖确保 CI 环境无需 MySQL |
| 安全审查 | 后端安全审查：修复 IDOR 越权漏洞（acceptTask）、权限提升漏洞（login 接口 role 字段）、Docker 明文密码修复为环境变量、JWT 真实签发逻辑、安全审查文档编写 |
| Docker | 后端多阶段 Dockerfile（eclipse-temurin:17），非 root spring 用户运行，.dockerignore 配置，MySQL 健康检查 + depends_on 启动顺序控制，Docker Secrets 密钥管理 |
| 云服务部署 | Vercel + Railway 多平台部署方案调研，vercel.json 配置文件编写，后端环境变量配置，协助排查 CORS 跨域和 Maven 编译问题 |
| 监控 | 后端监控基础设施参与，协助环境变量配置 |

---

## 九、技术栈总览

| 层次 | 技术选型 |
|------|----------|
| **前端框架** | React 18 + TypeScript |
| **构建工具** | Vite |
| **CSS 方案** | Tailwind CSS 4 + PostCSS |
| **UI 组件库** | shadcn/ui (Radix UI) |
| **路由** | React Router |
| **HTTP 客户端** | Axios |
| **Mock 工具** | MSW (Mock Service Worker) |
| **测试框架** | Vitest + @testing-library/react + user-event |
| **XSS 防护** | DOMPurify |
| **后端框架** | Spring Boot 3.2 (Java 17) |
| **ORM** | MyBatis-Plus |
| **数据库** | MySQL 8.0 |
| **认证** | JWT (jjwt) + BCrypt (jbcrypt) |
| **AI 模型** | DeepSeek-V3 (deepseek-chat) |
| **日志** | Logback + LogstashEncoder (JSON 格式) |
| **容器化** | Docker + Docker Compose |
| **CI/CD** | GitHub Actions |
| **代码覆盖率** | Codecov (前后端双 flag) |
| **安全扫描** | Gitleaks (密钥泄露) + Trivy (镜像漏洞) |
| **云平台** | 阿里云 ECS (Ubuntu 22.04) |
| **Web 服务器** | Nginx (Alpine) |

---

## 十、项目成果亮点

1. **完整的全栈工程闭环**：从 UI 原型 → 架构设计 → 前后端实现 → AI 集成 → 测试覆盖 → CI/CD → 安全加固 → Docker 容器化 → 云服务部署 → 可观测性监控，覆盖软件工程全生命周期
2. **高测试覆盖率**：前端 83.47% 语句覆盖率，远超 50% 要求
3. **OWASP 安全实践**：发现并修复 4 个高危漏洞、1 个中危漏洞，配置 2 道自动化安全扫描防线
4. **AI 深度集成**：DeepSeek-V3 驱动的 AI 页面导览 + AI 辅助开发全链路（代码生成、安全审查、测试编写）
5. **容器化与一键部署**：多阶段构建优化镜像体积，开发/生产/服务器三套 Compose 方案，非 root 安全运行
6. **可观测性基础**：健康检查 + 结构化 JSON 日志 + 请求指标（请求数/响应时间/错误率）

---

