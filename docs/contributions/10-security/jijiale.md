# 安全审查贡献说明

姓名：纪嘉乐
学号：2312190109
日期：2026-05-06

## 我完成的工作

### AI 安全审查
- **审查了哪些文件/模块**：前端 API 请求模块 (`src/app/api/client.ts`, `auth.ts`) 以及核心数据展示组件 (`src/app/components/AIPageSummary.tsx`, `TaskCard.tsx`)。
- **AI 发现的主要问题**：
  1. `client.ts` 中错误处理在生产环境可能暴露内部信息；Token 存储在 localStorage 存在潜在风险。
  2. `AIPageSummary.tsx` 中如果未来直接渲染 AI 输出的富文本，存在高危 XSS 注入风险。
- **我修复了哪些问题**：
  1. 优化了 Axios 拦截器，屏蔽了生产环境的详细错误输出，并封装了 Token 存取逻辑，增加了防 CSRF 探测请求头。
  2. 引入了 `DOMPurify` 库，在 `AIPageSummary.tsx` 中编写了 `createSafeHTML` 函数，彻底过滤恶意标签，防范了 XSS 漏洞。

### 安全检查清单
- [×] **密码存储**：(后端实现) 前端确保在 HTTPS 下安全传输。
- [✔] **JWT/Session**：已在请求拦截器中配置 `Bearer Token` 携带机制，并处理了 401 过期登出逻辑。
- [✔] **接口鉴权**：全局拦截器统一处理，保障请求安全。
- [✔] **越权访问**：(适用后端逻辑，前端配合隐藏越权 UI)。
- [×] **SQL 注入防护**：(不适用前端)。
- [✔] **XSS 防护**：已严格审查前端输出，特别是 AI 动态输出部分，已强制引入 `DOMPurify` 进行净化。
- [✔] **API Key/密码**：已将所有敏感配置从代码中抽离至 `.env`，并未包含在代码仓库中。
- [✔] **.env 文件**：已检查 `.gitignore`，确保完全忽略 `.env`，并在项目中提供了安全的 `.env.example` 模板。
- [✔] **依赖安全**：运行了 `npm audit`，并处理了部分依赖漏洞。

### CI 安全扫描
- **配置了哪个选项**：选项 A (密钥泄露扫描 Gitleaks)
- **扫描结果**：成功在 GitHub Actions 中集成，扫描结果为无密钥泄露，流水线顺利通过（绿勾）。

### 选做完成情况
- **安全 HTTP 头**：在前端拦截器层面增加了 `X-Requested-With` 等基础安全标识。

## PR 链接
- PR #X: [【在这里粘贴你提交到 GitHub 的 Pull Request 链接】](https://github.com/AASSKK99881/Help-1/pull/45)

## 遇到的问题和解决
1. **问题**：在执行前端依赖漏洞扫描（npm audit）并尝试修复时，发现 `vite` 等深层构建依赖存在版本锁定冲突，强行 `npm audit fix --force` 会导致云端 CI 依然报错（exit code 1）。
   **解决**：根据实际情况和作业选项规范，果断将 CI 扫描策略切换为“选项 A：Gitleaks 密钥泄露扫描”，在保证代码仓库安全合规（无硬编码密钥）的同时，使流水线成功跑通。

## 心得体会
在 Vibe Coding 场景下，AI 虽然极大提升了我们的开发效率，但也可能盲目生成“能跑就行”但不安全的代码（例如随意使用 `innerHTML`）。平衡效率与安全的关键，在于我们要完成身份的转变：从“敲代码的人”变成“审查者”。我们需要利用 AI 来反向“查杀”AI 写的代码，并在架构上提前做好防线（如引入 DOMPurify、隔离环境变量、配置 CI 自动化扫描）。只有把安全自动化融入到工作流中，才能在高速开发时没有后顾之忧。